import "./style.css";
const Loading = (loader) => {
    return (
        <div className="spinner-container m-5 p-5">
            <span class="loader"></span>
        </div>

    );
};

export default Loading;
